# Disable automatic audit during npm install
# Vulnerabilities in this project are predominantly from dev tooling
# (jest, eslint, nodemon) which are not present in production Docker images.
# Run `npm audit --omit=dev` to audit production-only dependencies.
audit=false
